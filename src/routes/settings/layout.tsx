import { ParentComponent } from "solid-js"

export const SettingsLayout: ParentComponent = (props) => {

    return (
        <>
            SettingsLayout
            {props.children}
        </>
    )
}
